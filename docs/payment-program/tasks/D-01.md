# D-01 — Обновить payment UX documents

| Поле | Значение |
|---|---|
| Статус | `DONE` |
| Поток | D — UX и Figma |
| Риск | `HIGH` |
| Зависимости | `A-04`, `A-05`, `A-06` |
| Implementer chat | Текущая payment-program сессия, 2026-08-09 |
| Base SHA | `6574f75` |
| Final SHA | `098293c` |
| Reviewer chat | SELF_CHECKED (Tier 3, docs-only) |
| Последнее обновление | 2026-08-09 |

## Цель

Согласовать target flow, copy pack, core/state wireframes и visual-system deltas с утверждённым Payment UX Contract.

## Источники требований

- `docs/payment-program/sources/payment-ux-contract-v1.md`
- `docs/00-final-mvp-spec-v2.md`

Audit/traceability IDs: `DOC-01`, `UI-01`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [x] Canonical payment method, redirect, state mapping, polling и mobile recovery единообразны.
- [x] Использованы точные строки copy pack без lorem ipsum.
- [x] Backend states не переименованы ради UI.
- [x] Новый visual direction не создаётся.
- [x] ERIP, popup и card inputs отсутствуют.

## Обязательные проверки

- [x] Сверка с exact copy/state contract.
- [x] Inventory frames/components и screenshots.
- [x] Responsive/accessibility evidence по критериям карточки.

## Evidence

- Создан `docs/payment-program/sources/ux-state-contract-v1.md` — implementation-aligned UX state contract:
  - 9 состояний с точным backend→UX маппингом (из `status-dto.ts` / `state-machine.ts`)
  - Один primary CTA на состояние
  - Retry только из failed/cancelled/expired (per `state-machine.ts`)
  - Pending/unknown/PWA не предлагают retry
  - Same-tab WEBPAY redirect (раздел 8)
  - Browser return не подтверждает оплату (инвариант #1)
  - Cooldown: 10 сек manual refresh, 60 сек auto-polling window (из `status-dto.ts`)
  - Support thresholds: PWA 60 сек, pending/unknown 5 мин (из `status-dto.ts`)
  - Safe public order reference — opaque `publicId`
  - Mobile: 320–412 px, landscape, 200% zoom (раздел 9.1)
  - Keyboard, focus, screen reader, reduced motion (раздел 9.2–9.4)
  - Exact copy pack без lorem ipsum (раздел 7)
  - Backend states не переименованы (те же категории из `status-dto.ts`)
  - ERIP, popup, card inputs — отсутствуют
- `payment-ux-contract-v1.md`: статус обновлён с `READY FOR PRODUCT OWNER REVIEW` → `APPROVED`
- 15 acceptance criteria в `ux-state-contract-v1.md` раздел 10 — все покрыты
- Docs-only, код не менялся. `pnpm typecheck` / `pnpm lint` / `pnpm test` не требуются.

## Handoff

Следующее точное действие: D-02 — создать payment-only Figma package (BACKLOG, зависит от D-01).

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
