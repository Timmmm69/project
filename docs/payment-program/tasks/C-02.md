# C-02 — Реализовать Order/session/redirect handoff

| Поле | Значение |
|---|---|
| Статус | `NEEDS_REVALIDATION` |
| Поток | C — Frontend |
| Риск | `HIGH` |
| Зависимости | `B1-05`, `D-02` |
| Implementer chat | Не назначен |
| Base SHA | `Заполняется при claim` |
| Final SHA | Не зафиксирован |
| Reviewer chat | Не назначен |
| Последнее обновление | 2026-07-18 |

## Цель

Реализовать creating_order, creating_payment_session, redirecting и безопасный 10-second fallback.

## Источники требований

- `docs/payment-program/sources/payment-ux-contract-v1.md`

Audit/traceability IDs: `PAY-04`, `PAY-05`, `UI-01`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [ ] CTA немедленно блокирует повторный submit.
- [ ] Один checkout_flow создаёт не более одного Order.
- [ ] Existing provider session переиспользуется.
- [ ] Canonical navigation — same-tab redirect.
- [ ] Fallback появляется только после подтверждённого session creation и не создаёт новую session.

## Обязательные проверки

- [ ] `pnpm lint`.
- [ ] `pnpm typecheck`.
- [ ] `pnpm test`.
- [ ] Релевантные Playwright/browser scenarios и ручная visual check.

## Evidence

- Ссылки на изменённые файлы/коммиты.
- Вывод обязательных проверок.
- Runtime/design/external evidence согласно карточке.
- Отдельный `reviews/C-02.md` перед `DONE`.

## Handoff

Следующее точное действие: Реализовать stateful handoff component и e2e duplicate/reload/fallback tests.

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
