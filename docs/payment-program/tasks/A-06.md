# A-06 — Сверить analytics measurement plan

| Поле | Значение |
|---|---|
| Статус | `DONE` |
| Поток | A — Управление и документация |
| Риск | `HIGH` |
| Зависимости | `A-03`, `A-04` |
| Implementer chat | Текущая goal-сессия, 2026-07-31 |
| Base SHA | `c137ba6` |
| Final SHA | `96e95f864dc5fc88a46a28ee86cc5ca68d78c07d` |
| Reviewer chat | `/root/a06_reviewer` |
| Последнее обновление | 2026-07-31 |

## Цель

Создать или актуализировать measurement plan и реестр обязательных backend truth events без изменения privacy boundary.

## Источники требований

- `docs/payment-program/sources/payment-ux-contract-v1.md`
- `docs/payment-program/sources/payment-core-audit-2026-07-18.md`
- `docs/00-final-mvp-spec-v2.md`

Audit/traceability IDs: `ANA-01`, `ANA-02`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [x] Каждое обязательное payment/access event имеет authority, producer и разрешённый payload.
- [x] CTA/return/browser state не могут создать `payment_confirmed`.
- [x] Email, email hash, card data, provider secrets/references и raw URL запрещены.
- [x] Analytics failure не блокирует checkout, verification или Access grant.
- [x] Отсутствующие producers связаны с B3-05.

## Обязательные проверки

- [x] Проверка ссылок/путей и source hierarchy.
- [x] Поиск orphan/duplicate requirements и противоречий.
- [x] Кодовые проверки не обязательны: runtime/schema/API не менялись.

## Evidence

- `docs/payment-program/analytics-measurement-plan-v1.md` содержит 14
  обязательных событий с authority, emission boundary, strict payload allowlist,
  baseline status и владельцем gap.
- Текущий registry/callsites сверены по:
  `src/lib/analytics/schemas.ts`,
  `src/lib/analytics/forbidden-payload.ts`,
  `src/lib/analytics/analytics-service.ts`,
  `src/lib/analytics/analytics-id.ts`,
  `src/lib/commercial/commercial-service.ts` и
  `tests/unit/analytics.test.ts`.
- Реестр явно фиксирует 4 implemented, 2 partial и 8 missing events.
- Для `checkout_started` зафиксирован mismatch между backend flow creation и
  доказанным UI render.
- Для `order_created` зафиксировано, что текущие `amount`/`currency`
  противоречат privacy boundary контракта; исправление направлено в `B3-05`.
- Все missing/partial producers связаны с `B3-05`; PWA-derived events также
  зависят от `B2-03`.
- CTA/return/browser/URL/frontend loader/provider screenshot явно не могут
  создавать `payment_confirmed`.
- Email и email hash, card data, provider secrets/references, точная
  сумма/валюта и raw URL запрещены.
- Analytics failure isolation и deterministic transition idempotency закреплены
  как обязательные invariants.
- Source register содержит owner/version/status measurement plan.
- Проверка реестра подтверждает 14 уникальных обязательных event names и наличие
  всех шести текущих schema events.
- Проверка task references подтверждает существование `B2-03`, `B2-02` и
  `B3-05`.
- Runtime/schema/API не менялись; `pnpm lint`, `pnpm typecheck`, `pnpm test` и
  Playwright для docs-only diff не запускались.
- `git diff --check` выполняется перед commit.
- Отдельный `reviews/A-06.md` обязателен перед `DONE`.
- Independent review `reviews/A-06.md`: `DONE`; `git diff --check c137ba6 96e95f8` — PASS.

## Handoff

Следующее точное действие: `B3-05` реализует authoritative analytics producers
и privacy-negative tests; `D-01` может быть заявлена как независимая UX-documentation задача.

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
