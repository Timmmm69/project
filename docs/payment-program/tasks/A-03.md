# A-03 — Повторно проверить 35 audit findings

| Поле | Значение |
|---|---|
| Статус | `IN_REVIEW` |
| Поток | A — Управление и документация |
| Риск | `CRITICAL` |
| Зависимости | `A-02` |
| Implementer chat | Текущая goal-сессия, 2026-07-30 |
| Base SHA | `b172f70` |
| Final SHA | Атомарный A-03 commit, содержащий это evidence; reviewer берёт SHA из HEAD |
| Reviewer chat | Требуется отдельный независимый reviewer |
| Последнее обновление | 2026-07-30 |

## Цель

Сверить каждую строку backend gap matrix с актуальным HEAD и заменить исторические статусы текущими evidence-backed статусами.

## Источники требований

- `docs/payment-program/sources/payment-core-audit-2026-07-18.md`
- `docs/00-final-mvp-spec-v2.md`
- `docs/payment-program/sources/payment-ux-contract-v1.md`

Audit/traceability IDs: `ORD-01..06`, `PAY-01..07`, `ACC-01..02`, `STA-01..07`, `REC-01..03`, `SEC-01..03`, `CARD-01..02`, `ANA-01..02`, `DOC-01..02`, `UI-01`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [x] Все 35 audit IDs проверены на актуальном SHA.
- [x] Для каждого ID указаны текущие файлы/тесты и один статус.
- [x] `IMPLEMENTED` требования превращены в regression invariants, а не удалены.
- [x] Все `NEEDS_REVALIDATION` карточки переведены в обоснованный следующий статус.
- [x] Ни один merchant-blocked пункт не закрыт mock/sandbox assumption.

## Обязательные проверки

- [x] Проверка ссылок/путей и source hierarchy.
- [x] Поиск orphan/duplicate requirements и противоречий.
- [x] Кодовые проверки не обязательны: runtime-код не менялся.

## Evidence

- Revalidation matrix: `docs/payment-program/audit-revalidation-2026-07-30.md`.
- Baseline finding: historical `adf2355` не является предком current main; merge base — `01eb2d3a8a52e5d7efe261e88cacce378212037e`.
- Current branch не содержит verified-student-session/recovery/canonical analytics runtime из audited sibling branch; статусы не копировались из historical audit.
- Проверены 35/35 уникальных IDs:
  - `IMPLEMENTED = 11`;
  - `PARTIAL = 11`;
  - `MISSING = 9`;
  - `CONTRADICTED = 2`;
  - `MERCHANT_BLOCKED = 2`.
- Regression invariants сохранены для 11 implemented IDs.
- Все 24 карточки `NEEDS_REVALIDATION` переведены: 23 в `BACKLOG` до A-03 verdict, `B2-04` в `BLOCKED_EXTERNAL` из-за `E-02`.
- После `A-03 = DONE` reviewer открывает 12 dependency-ready карточек, перечисленных в revalidation matrix.
- Board/card status reconciliation и orphan/duplicate checks выполняются перед commit.
- Runtime/schema/API/tests не изменялись; `pnpm lint`, `pnpm typecheck`, `pnpm test` не запускались для documentation-only revalidation.
- Отдельный `reviews/A-03.md` обязателен перед `DONE`.

## Handoff

Следующее точное действие: отдельный reviewer повторяет ancestry, 35-ID/status counts, выборочные code/absence checks, card routing и merchant gates; reviewer не исправляет implementation.

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
