# B3-04 — Удалить raw provider payload persistence

| Поле | Значение |
|---|---|
| Статус | `DONE` |
| Поток | B3 — Security и analytics |
| Риск | `CRITICAL` |
| Зависимости | `A-03` |
| Implementer chat | Этот чат |
| Base SHA | `5656009` |
| Final SHA | `0c230f7` |
| Reviewer chat | Не назначен |
| Последнее обновление | 2026-08-09 |

## Цель

Сохранить только allowlisted нормализованные provider данные или безопасный hash; legacy слой изолировать от canonical checkout.

## Источники требований

- `docs/payment-program/sources/payment-ux-contract-v1.md`
- `docs/payment-program/sources/payment-core-audit-2026-07-18.md`
- `docs/00-final-mvp-spec-v2.md`

Audit/traceability IDs: `CARD-02`, `CARD-01`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [x] Commercial и затронутый legacy code не сохраняют raw request/response payload.
- [x] Allowlist исключает PAN/masked PAN/CVV/expiry/3DS/signatures/secrets.
- [x] Existing stored payload policy и migration/retention определены.
- [x] Analytics/logging не получают raw provider data.
- [x] Static/runtime privacy scans покрывают callback/create/status paths.

## Обязательные проверки

- [x] `pnpm lint`.
- [x] `pnpm typecheck`.
- [x] `pnpm test` плюс targeted unit/integration tests.
- [x] Релевантный migration/transaction/concurrency smoke.

## Evidence

- `src/lib/commercial/payload-sanitizer.ts` — `sanitizeProviderPayload()` рекурсивно удаляет все forbidden ключи (PAN, masked PAN, CVV, expiry, 3DS, signatures, secrets, session tokens, raw body/request/response, payment URL, credentials). `containsForbiddenKeys()` для runtime privacy scan.
- `src/lib/payments/payment-service.ts` — `providerResponseData()` и `applyPaymentStatusUpdate()` применяют сантайзер к `providerPayload`, `providerWebhookPayload` и event_logs перед записью.
- `prisma/migrations/20260809123000_sanitize_payment_payloads/` — миграция: `UPDATE payments SET provider_payload_json = NULL, provider_webhook_payload_json = NULL`.
- `tests/unit/commercial-payload-sanitizer.test.ts` — 16 тестов: forbidden ключи на любой глубине, массивы, case-insensitive, allowed поля сохраняются.
- Canonical `commercial_payment_events.redactedPayload` уже allowlisted (`redactProviderPayload`); analytics защищены `forbidden-payload.ts`.
- Legacy payment code изолирован от canonical WEBPAY checkout — разные таблицы, enum, service layers.
- 499 tests PASS, typecheck/lint clean.

## Handoff

Следующее точное действие: Провести inventory persisted payloads, выбрать delete/sanitize policy и реализовать migration/tests.

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
