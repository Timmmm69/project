# A-07 — Поддерживать полную traceability

| Поле | Значение |
|---|---|
| Статус | `IN_PROGRESS` |
| Поток | A — Управление и документация |
| Риск | `HIGH` |
| Зависимости | `A-03` |
| Implementer chat | Текущая goal-сессия, 2026-07-31 |
| Base SHA | `68e48c1` |
| Final SHA | Не зафиксирован |
| Reviewer chat | Не назначен |
| Последнее обновление | 2026-08-09 |

## Цель

Поддерживать соответствие между источниками, карточками, acceptance criteria и фактическим evidence до финального gate.

## Источники требований

- `docs/payment-program/sources/payment-ux-contract-v1.md`
- `docs/payment-program/sources/payment-core-audit-2026-07-18.md`
- `docs/00-final-mvp-spec-v2.md`

Audit/traceability IDs: `ALL`.

## В scope

- Реализовать только цель и критерии этой карточки.
- Сохранить backend truth, idempotency, transaction и privacy invariants.
- Обновить board, evidence и handoff вместе с изменением.

## Вне scope

- Реализация соседних карточек без отдельного claim.
- Production activation или изменение gate verdict.
- Card-data UI, embedded bank form, автоматические возвраты и другие запрещённые MVP-функции.


## Критерии приёмки

- [x] На checkpoint 2026-07-31 каждый audit ID и acceptance criterion связан
  минимум с одной карточкой или regression invariant.
- [x] На checkpoint 2026-07-31 все 20 provider dependencies и legal/ops gates
  имеют владельца и статус.
- [x] Figma frames/components/constraints/evidence полностью направлены в
  D-01..D-03 и C-06.
- [x] Принятые A-01..A-06 имеют SHA, evidence, синхронные board counts и review
  reports.
- [x] На текущем checkpoint нет orphan requirements или `DONE` без review
  report.

Эти отметки подтверждают текущий checkpoint, а не будущую финальную приёмку.
A-07 остаётся `IN_PROGRESS` и закрывается независимым reviewer только перед
QA-02 после повторения тех же проверок на финальном HEAD.

## Обязательные проверки

- [x] Проверка ссылок/путей и source hierarchy.
- [x] Поиск orphan/duplicate requirements и противоречий.
- [x] Кодовые проверки не обязательны: runtime/schema/API не менялись.

## Evidence — checkpoint 2026-07-31

- `board.md` содержит 45 уникальных task rows и согласованные counts:
  17 `BACKLOG`, 11 `READY`, 1 `IN_PROGRESS`, 10 `BLOCKED_EXTERNAL`, 6
  `DONE`.
- Audit matrix: 35 уникальных IDs; revalidated status/evidence остаётся в
  `audit-revalidation-2026-07-30.md`.
- Payment UX acceptance matrix: 32 уникальных criteria IDs.
- Provider matrix: 20 уникальных dependencies, каждый с owner, task routing и
  `BLOCKED_EXTERNAL`.
- Legal/operations matrix содержит owner, task routing и gate status для каждой
  зависимости.
- Figma matrix покрывает 19 desktop + 19 mobile frames, 320 px, полный component
  inventory, constraints и восемь видов acceptance evidence.
- Все 45 registry rows разрешаются в существующие `tasks/<id>.md`.
- Все шесть текущих `DONE` карточек A-01..A-06 имеют отдельный review report.
- Последний независимо принятый program state перед claim A-07: `68e48c1`.
- Traceability checkpoint обновлён через B1-03: `ORD-06`, `REC-03` и `SEC-01`
  связаны с принятыми resolver/continuation/guard evidence (`7b94ab2`,
  `reviews/B1-03.md`).
- B1-04 принят independent review (`df106dd`, `reviews/B1-04.md`) и закрывает
  runtime части `ORD-03`, `ORD-04` и pre-verification `SEC-01`; UI handoff
  остаётся в C-01.
- B1-05 принят independent review (`4a6a013`, `reviews/B1-05.md`) и закрывает
  server/runtime части `ORD-05` и `ORD-06`: safe pending reference/action без
  token rotation, Existing Access/Attempt/Result action и no-repurchase. Full
  recovery payment/status continuation остаётся B2-07, browser presentation —
  C-02/C-05.
- B2-01 принят independent consolidated review (`20adce9`,
  `reviews/B2-01.md`): `ORD-01`, `ORD-02` и server/runtime часть `CHK-01`
  получили compatible DB snapshot migration, snapshot-based Access grant и
  Product-mutation regression. UI-представление `CHK-01` остаётся в C-01.
- B2-02 claimed 2026-08-02 от `e590b3c`; runtime/API evidence для `STA-02` и
  `STA-03` будет зафиксировано в карточке перед consolidated B2 review.
- B2-02 implementation завершена в `IN_REVIEW`: `STA-02` получает transient
  unknown projection без DB enum, а `STA-03` сохраняет authoritative provider
  boundary; final SHA и verdict фиксирует consolidated B2 reviewer.
- B2-06 claimed 2026-08-02 от `a86d4f6`; safe DTO evidence для `STA-06` и
  `STA-07` будет зафиксировано перед consolidated B2 review.
- B2-06 implementation завершена в `IN_REVIEW`: `STA-06/STA-07` получают
  strict public serializer, deterministic support thresholds и negative
  sensitive-field coverage; final SHA/verdict фиксирует B2 reviewer.
- B2-03 claimed 2026-08-02 от `0a7c69e`; `ACC-02` reconciliation/concurrency
  evidence будет зафиксировано перед consolidated B2 review.
- B2-03 implementation завершена в `IN_REVIEW`: `ACC-02` получает PAID-only
  snapshot reconciliation, exactly-one concurrency evidence, 60-second support
  escalation и canonical detected/resolved analytics без refund.
- B2-05 claimed 2026-08-02 от
  `fb1f926f73ee6ef4031b897072f7988f3d0f91a5`; evidence для `PAY-03` и
  `STA-01` будет зафиксировано перед consolidated B2 review.
- B2-05 implementation завершена в `IN_REVIEW`: `PAY-03/STA-01` получают
  explicit terminal-only retry contract, same-Order retry и PostgreSQL
  concurrency evidence одной active PaymentAttempt; browser return не является
  proof of payment.
- B2-07 claimed 2026-08-02 от
  `5f8ba76deb9a705dd22bb7e5f995d6bc243681c0`; read-only recovery evidence для
  `REC-03/REC-01` будет зафиксировано перед consolidated B2 review.
- B2-07 implementation завершена в `IN_REVIEW`: `REC-03/REC-01` получают
  verified payment_pending/PWA projections, read-only repeated/concurrent
  resolver evidence и manual-refresh proof без создания Order/PaymentAttempt/Access;
  ACC-01A destination guards и enumeration resistance сохранены.
- C-01 реализован `DONE` (pending consolidated C review): `UI-01/ORD-03` — checkout
  hierarchy с recovery email verification (OTP), точной copy (UX contract раздел 11),
  двумя required checkboxes, WEBPAY redirect notice и «Перейти к оплате картой» CTA.
  Информационная иерархия: price/one-attempt → constraints → identity/payment → legal/trust.
  C-02 разблокирован (BACKLOG → READY).
- Mock/local/sandbox assumptions нигде не переводят merchant dependencies из
  `BLOCKED_EXTERNAL`.
- `git diff --check` выполняется перед checkpoint commit.
- Runtime/schema/API не менялись; lint/typecheck/test/Playwright для docs-only
  checkpoint не запускались.
- Финальный `reviews/A-07.md` создаётся только перед QA-02; до этого A-07
  сохраняет `IN_PROGRESS`.

## Handoff

Следующее точное действие: после commit B3-02, реализовать B3-03 (cache/referrer). После каждого принятого review обновлять матрицы A-07; закрывать
A-07 только перед QA-02.

Нерешённые вопросы должны быть перечислены здесь и в `docs/payment-program/handoff.md`; молчаливые архитектурные, финансовые, security или scope решения запрещены.
