# Review B2-01

| Поле | Значение |
|---|---|
| Reviewer | Независимый consolidated reviewer `/root/b201_review` |
| Reviewed SHA | `20adce9` |
| Base SHA | `7068ad5` |
| Дата | 2026-08-02 |
| Verdict | `DONE` |

## Consolidated HIGH-risk migration/payment review

Проверен полный bounded diff `7068ad5..20adce9` для B2-01, включая Prisma
schema/migration, создание Order, paid callback и recovery resolver. Base SHA
является предком reviewed SHA. Это независимый consolidated review всего
immutable snapshot milestone, а не review его отдельных подшагов.

## Spot-check результата

- Migration добавляет шесть nullable snapshot-полей, backfill-ит их из
  связанного Product/Test, fail-closed прерывает миграцию при неполном
  backfill, затем переводит поля в `NOT NULL` и добавляет positive/known-value
  DB constraints. Тем самым старый Order не получает частично угаданные terms.
- Создание нового Order проверяет фиксированные backend-owned цену, валюту,
  лимит попыток, окно старта, retention и timer, затем записывает полный
  immutable snapshot вместе с существующими legal-version полями. Клиент не
  определяет цену или валюту.
- В authoritative paid transaction Access получает `testIdSnapshot`, число
  попыток и start deadline из Order snapshot; callback replay остаётся под
  существующими locks, conditional transitions и unique `commercialOrderId`
  Access boundary. Product mutation между Order и callback не меняет выданные
  попытки или 90-day start window.
- Existing entitlement/recovery сверяет купленные snapshot terms и использует
  Order retention, а не mutable Product terms; неконсистентная purchased truth
  fail-closes в `support_required`. Изменения не открывают browser payment
  confirmation, не ослабляют privacy boundary и не активируют provider.

## Использованное evidence и проверки

- Recorded evidence в B2-01: isolated PostgreSQL 14→15 migration с реальным
  old Order — PASS, все шесть полей backfilled; temporary `b201_ci` schema
  удалена. Commercial DB regression — 15/15 PASS, включая Product/Test
  mutation до paid callback.
- Recorded lint/typecheck — PASS. Full regression evidence — 404 PASS,
  139 DB-gated skipped в обычном запуске.
- В review повторены `git diff --check` — PASS, ancestry
  `7068ad5 ⊆ 20adce9` — PASS и `pnpm test` — 404 PASS / 139 skipped; его
  targeted-argument form запускает полный Vitest suite в текущем script.
- Prisma CLI validation отдельно не выполнена: доступный `pnpm` environment
  не содержит исполняемый `prisma`. Это не обязательный недостающий evidence:
  schema/migration уже прошли recorded lint/typecheck и isolated PostgreSQL
  migration smoke.

## Решение

Новых CRITICAL/HIGH findings и обязательных пробелов evidence нет. B2-01
принимается как bounded immutable commercial snapshot milestone. Следующий
READY block — B2-02 только после отдельного claim; production gate остаётся
`NO-GO`.
