# Review B1-04

| Поле | Значение |
|---|---|
| Reviewer | Независимый consolidated reviewer `/root/b102_milestone_review` |
| Reviewed SHA | `df106dd` |
| Base SHA | `dcf58d3` |
| Дата | 2026-08-01 |
| Verdict | `DONE` |

## Consolidated critical-milestone review

Это один завершающий Tier-1 independent review всего B1-04 verified-email/order
boundary milestone, а не review внутренних подшагов. Проверены diff
`dcf58d3..df106dd`, canonical POST command, authority/order transaction and
concurrency paths, compatibility boundary and evidence. Дорогие проверки и DB
smokes намеренно **не запускались повторно**: использовано зафиксированное в
карточке implementation evidence.

## Spot-check результата

- Canonical `POST /api/commercial/orders` uses a strict verified-order schema
  that excludes `email`; a supplied email is rejected before Order work.
- Route requires an enabled and available recovery runtime plus the opaque
  recovery cookie before it invokes Order service. Off, shadow/unavailable or
  missing authority fail closed, without checkout-state discovery.
- Service derives `emailNormalized` and stored email only from validated
  server authority. Authority is validated again inside the main Order
  transaction before checkout/order/access/attempt reads, with exact current
  Product/Test scope; invalid, expired and cross-scope authority yields the
  safe `VERIFIED_EMAIL_REQUIRED` outcome.
- P2002 concurrent-create recovery runs in its own transaction and repeats the
  authority/scope validation before same-flow/open-order queries. Existing
  checkout-flow/idempotency, server price/currency and legal assertions remain
  authoritative; no duplicate Order is created for the same flow.
- The legacy email-capable service entry remains for existing non-HTTP test
  harnesses only: static source search found no production `src/` caller other
  than the canonical route, which supplies no client email and always provides
  verified authority.
- No migration was added or altered. Production activation is not introduced;
  production remains `NO-GO`, and UI for acquiring verified email authority is
  explicitly deferred to C-01.

## Использованное evidence

- HTTP authority boundary: 4/4 PASS; related targeted unit: 3 files, 25/25
  PASS.
- Fresh dedicated 14-migration schema: extended recovery/Order integration
  24/24 PASS, including spoofed email, missing/invalid/expired/cross-product
  authority, same-flow concurrency and exactly-one Order; schema removed.
- Lint and typecheck — PASS; full regression: 26 files, 399 tests PASS (139
  database-gated tests skipped in the ordinary run). `git diff --check` is PASS.
- No production activation was performed; production remains `NO-GO`.

## Решение

Новых findings нет. B1-04 принимается как bounded verified-email/order
authority milestone. B1-05 is unblocked and moves to `READY` for existing
Order/Access/Attempt/Result recovery; no production gate is closed.
