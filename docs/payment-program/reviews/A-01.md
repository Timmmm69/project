# Review A-01

| Поле | Значение |
|---|---|
| Reviewer chat | Текущий независимый review chat |
| Reviewed SHA | `dfec322fbbfac0a6a7318a103c69a319dbc8f8cb` |
| Дата | 2026-07-18 |
| Verdict | `CHANGES_REQUIRED` |

## Проверенный scope

- Обязательные источники: `AGENTS.md`, `docs/00-final-mvp-spec-v2.md`, board, A-01, handoff, source register, review protocol и оба импортированных source-файла.
- Implementation diff от `80c6838ce54e8e0768b4264698343e98be7cbaea` до reviewed SHA.
- Реестр/карточки, статусы, ссылки, зависимости и traceability matrices.
- Source provenance/equivalence, hierarchy, production gate, feature-implementation gate, secret scan и отсутствие изменений payment-кода.

## Findings

### HIGH — SRC-EQ-01: обе импортированные source copies не эквивалентны оригиналам после нормализации только line endings

Зарегистрированные оригиналы доступны, и их raw SHA-256 точно совпадают с `sources/README.md`:

- `C:\Users\novik\Downloads\payment-ux-contract-v1.md` — `557344B6C84DDC8B9F2767F72108A040D5D666A912EC5AF6FE0D6806D39D9DBF`;
- `C:\Users\novik\.codex\attachments\8c9b0de7-7486-4666-88ae-705be8bb1a14\pasted-text.txt` — `976AF910B17A76932E6B1B1433C5BF8F27ABFA622605CE9E5D9298CC2A7A7C2C`.

После нормализации `CRLF` и lone `CR` в `LF` comparison остаётся неравным:

- UX source: original length `53850`, imported length `53852`, первый diff на EOF original (`53850`); imported copy заканчивается тремя LF вместо одного.
- Audit source: original length `28043`, imported length `28045`, первый diff на EOF original (`28043`); imported copy заканчивается двумя LF, original — без завершающего LF.

Следовательно, заявления о текстовой эквивалентности в `sources/README.md`, A-01 evidence и исходном handoff не подтверждены. По Independent Review Protocol отсутствующее обязательное evidence требует `CHANGES_REQUIRED`.

Точное исправление для отдельного implementation pass:

1. Удалить ровно два последних LF из `sources/payment-ux-contract-v1.md`.
2. Удалить ровно два последних LF из `sources/payment-core-audit-2026-07-18.md`.
3. Не менять никакой другой символ в импортированных source files.
4. Повторить raw SHA original verification и ordinal comparison после нормализации `CRLF`/`LF`; обе пары должны стать exact equal.
5. Обновить evidence/status/handoff и передать отдельному reviewer. `A-02` до этого остаётся `BACKLOG`.

Иных actionable findings не обнаружено.

## Повторные проверки

- [x] Source/acceptance traceability: 35 audit IDs, 32 UX acceptance criteria, 20 provider dependencies, 11 legal/ops rows и Figma frames/components/evidence имеют существующих task owners; неизвестных/orphan task references нет.
- [x] Diff и affected interfaces: implementation commit добавляет только 53 файла в `docs/payment-program/`; payment/feature/schema/migration/API files не менялись, affected runtime interfaces отсутствуют.
- [x] Обязательные автоматические проверки: выполнены независимые структурные, graph, hash/equivalence и secret checks; `pnpm lint`, `pnpm test`, `pnpm typecheck` не запускались, потому что reviewed implementation и review pass меняют только Markdown-документацию и не затрагивают runtime code.
- [x] Security/privacy/concurrency invariants: backend truth, idempotency, transaction, card-data boundary и production `NO-GO` сохранены документально; high-confidence secret patterns не найдены.
- [x] Документация и handoff: review report создан, A-01/board/handoff согласованно переведены в `CHANGES_REQUIRED`; source copies намеренно не исправлялись в review pass.

## Evidence

- Task inventory: `45` task files, `45` board registry rows, duplicate/missing IDs — `0`; все 45 board task links существуют, headings совпадают.
- Status reconciliation до verdict: `NEEDS_REVALIDATION=24`, `BACKLOG=11`, `IN_REVIEW=1`, `BLOCKED_EXTERNAL=9`, остальные `0`; summary, registry и task files совпадали.
- Dependency graph: `45/45` nodes topologically resolved; unknown, self и cycle dependencies — `0`; board/task dependency mismatches — `0`.
- Traceability: source/board audit ID sets равны (`35`); source acceptance rows и board IDs — `32`; provider sequences — `1..20`; legal/ops rows — `11`; Figma traceability покрыта D-01/D-02/D-03 и downstream C/QA карточками.
- Source equivalence: оба raw original SHA совпали с register; normalized comparisons — `false` только из-за двух дополнительных EOF LF в каждой imported copy.
- Scope diff: `80c6838..dfec322` содержит только `docs/payment-program/`; payment-код не изменён.
- Secret scan: high-confidence private-key, cloud token, GitHub token, OpenAI-style token, JWT и assigned-secret patterns — совпадений `0`.
- Gate scan: source hierarchy, source reconciliation block и production `NO-GO` присутствуют в board/source register/handoff/tasks.

## Verdict rationale

Структура доски, зависимости, counts, traceability, legal/ops/Figma ownership, safety gates и diff scope прошли review. Однако текстовая эквивалентность обеих импортированных копий — явный критерий A-01 и обязательное evidence — не подтверждена. Поэтому карточка не может получить `DONE`.

## Следующее действие

Отдельный implementation chat выполняет только пять точных шагов из finding SRC-EQ-01, создаёт атомарный commit и возвращает `A-01` в `IN_REVIEW`. После нового независимого review при exact source equality A-01 может быть переведена в `DONE`, а `A-02` — в `READY`.
